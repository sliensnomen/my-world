# 世界观治理工具 · 项目文书

**日期：2026-09-22 ｜ 状态：协议草案 v0.2 已立，实现未冻结**
**本目录：** `PROTOCOL.md`（协议草案 v0.2）＋ `canonlint.py`（旧原型，待按 v0.2 重写）＋ 本文书

---

## 1. 一句话定位

**世界观治理协议 + 参考实现**：给多人共创世界一套机器可审计的治理规则，规则才是产品，AI 只是 lint 器的一种。

- 地板：git 语义原生的版本控制（已被 git-backed CMS 反复验证）；
- 护城河：审计协议（确定性 lint 规则集 + 宪法条款 + 政治经济学链条）。

## 2. 为什么不是 fork Kanka

- **法律死**：Kanka 是 Commons Clause + proprietary，禁止商用与托管服务，fork 无产品未来；
- **架构死**：内容在 MySQL 行里（HTML 正文 + 多态关联图），加 branch/PR 等于换地基而房子还立着；
- **维护死**：上游月均一个版本，动存储层的 fork 三个月内变成无法合并的化石。
- **活路**：Kanka 有官方 API 与 `Entity::export()`——不动它一行代码，把世界导出为协议条目仓库，我们的协议去审计它。审计署不需要拥有印刷厂。

## 3. 设计哲学（三个"合法化"）

同一个动作的三次复用，是全套设计的骨架：

1. **矛盾合法化**（叙事层）：矛盾不阻塞协作，标注 `conflicts_with` 并存，世界内降格为不可靠叙述——用治理哲学消解了 git 合并冲突的语义悬崖；
2. **工单化**（结构层）：改动不炸下游，CA504 把"改 A 炸 B"变成待复核队列；
3. **偏差合法化**（数值层）：AI 算出期望值，作者可偏离但须留原因——原因本身入库成为设定资产。

配套原则：**预防优于审计**（枚举用下拉消灭、向上连线禁画、id 系统生成——lint 是法庭，好产品让用户一辈子不上法庭）；**缺数据不报错，有数据才验算**。

## 4. 架构

```
普通用户：填卡 + 连线（不碰 YAML，不碰 git）
     │  UI 层消灭一半规则违规（预防优于审计）
     ▼  编译
存储层：git 仓库里的 Markdown + frontmatter（唯一事实来源，可 diff/回滚/CI）
     │
     ▼  审计
确定性 lint：建 id 索引 + 关系图 → 图算法 → 规则编号 + 退出码（协议判定可复现）
AI 建议层（CA9xx）：语义/数值合理性建议，永不进退出码
     │
     ▼  呈现
视图：关系图（链条投影 = 思维导图）＋ 地图（地理投影，coords 字段）
```

**图模型（codegraph 同构）：** 节点 = 条目（id 索引），边 = 三类引用（`canon_refs` 语义引用 / `conflicts_with` 矛盾 / `depends_on` 供养依赖）。CA 规则即图算法：CA501 层级方向校验、CA502 环检测、CA504 反向传递闭包。与 codegraph 的唯一区别：代码的边从源码自动抽取，世界的边由作者显式连线——这正是"填卡+连线"存在的原因。

## 5. 数据模型（WGP-core v0.2 摘要）

- **条目** = 一个 Markdown 文件；必填字段：`id`（全库唯一、永不更改）、`title`、`type`（5 值）、`author`、`date`、`status`、`load_bearing`、`canon_refs`、`conflicts_with`、`depends_on`、`superseded_by`、`ai_assisted`；
- **状态机**：`draft → trial → canon → archived`，另有恢复路径 `archived → canon`（仅评审）；降级必须填 `superseded_by` 并生成 CA504 工单，**无 stale 豁免**；
- **引用只认 id**，不认路径；
- **AI 条款**：AI 产出永不入 canon；辅助须永久标注；签字人即唯一责任人。

详见 `PROTOCOL.md`，规则注册表 CA1xx–CA5xx + CA9xx（AI 建议段）。

## 6. 政治经济学链条

- 七层字典（协议常量）：`geo → resource → production → economy → fiscal → military → political → ideology`；
- `depends_on` 边带 `kind`（6 值封版）与 `critical`（断供即崩塌）；目标链条序号必须 ≤ 自身；图必须无环；
- 承重墙条目由 `load_bearing: true` **显式声明**触发义务（缺字段 = CA302 error），关键词命中仅警告（CA301）；
- 产品形态：连线即供血线；分层视图看结构缺口；断供高亮；环高亮；改动传染预览；
- 边界：链条证明结构完整性，不证明数值自洽。

## 7. 数值系统（WGP-econ 扩展包 · 草案）

- 作者填数（`figures`，带单位）；参数表给硬边界（如"前现代常备军 ≤ 人口 2%"）；越界须留 `justification`，否则 CA6xx warning；
- `depends_on` 边将来可挂 `quantity` → 物资平衡表（已知供给 ≥ 已知需求），仅在数据自报的子图里验算；
- AI 层查软合理性（"关税养不养得起舰队"），`justification` 文本是 AI 复核输入；
- **参数表必须用巫王世界真实数据验证后才能封版。**

## 8. 协议分层（防膨胀）

| 包 | 内容 | 状态 |
|---|---|---|
| WGP-core | 条目/状态机/引用/矛盾/链条结构/CA1xx–CA5xx | v0.2 草案 |
| WGP-econ | figures/justification/参数表/quantity 边/CA6xx | 草案，待真实数据验证 |
| WGP-geo | coords 字段 | 随时可进 |
| 附录 C | 各 type 的 facet 建议清单（城市/宗教…），仅填卡辅助 | 建议性，永不进退出码 |

扩展包独立版本号，仓库声明启用哪些包，lint 只跑启用包的规则。

## 9. 路线图

1. **v0.1**：按 PROTOCOL.md 重写 `canonlint.py`（✅ 已完成，现同步 v0.5）；
2. **v0.2 dogfood**：魔戒/冰火 ✅（kind 分族、CA505 由此而来）；沙丘 ✅（生产↔资源的方向辨析、x-faith-engineering）；巫王世界数据待补；
3. **协议 v1.0 封版**：三个测试世界跑完 + kind 封版后；
4. **之后**：WGP-econ（figures/justification/参数表）、AI 建议层（CA9xx）、Kanka 导出导入器；
5. **前端最后搞**（2026-09-23 定）：方向定本地优先（git 藏进壳，协作走用户自己的 forge 账号），候选路线 fork Decap CMS 或 Obsidian 插件，协议封版前不动工。

## 10. 待决问题（v0.3 已全部裁决）

1. ~~kind 封版~~ → 不封版，六值为初始集，`x-` 实验值合法；进行中：3–5 个真实世界观供应链标注案例收集，巫王世界为首个样本；
2. ~~条目级权限~~ → 不入协议，落地为审计中立性条款（§6.2）：权限信息不得写入 frontmatter 影响判定；
3. ~~conflicts_with 方向性~~ → 默认对称 + 可选 `stance`（official/heretic/unknown），CA403 放宽；
4. ~~跨仓库引用~~ → `world:id` 语法保留不实现，遇到必报 CA401，禁止静默忽略；
5. ~~恢复冷却期~~ → 交宪章声明，默认路径 `archived → trial → canon`。

**当前无协议级阻塞项，lint 重写（v0.1）可以开工。**
